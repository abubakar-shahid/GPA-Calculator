// src/controllers/gpaController.ts

import { Course, Semester } from '../models/gpaModel';

export const calculateCGPA = (semesters: Semester[]): number => {
  // Implement CGPA calculation logic
  return 0;
};

export const calculateSGPA = (courses: Course[]): number => {
  // Implement SGPA calculation logic
  return 0;
};
